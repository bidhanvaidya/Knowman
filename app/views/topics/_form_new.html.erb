<%= javascript_include_tag :ckeditor %>
<table>
<%= form_for([@folder, @folder.topics.new], :remote => true ) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %><tr ><th align="center"><table><tr><th>
	<div class="field">
	<%= f.label :title %><br />
	<%= f.text_field :title %>
	</div></th><th align = left valign=bottom> <%=f.submit 'Save'%></th></tr></table></th></tr>

	<tr>
<td align = "center">
	<div class="field">

	<div class="field" >
			<%= f.cktext_area :content, :height => "900px", :width => '900', %>


		
</div>
</td>

<td valign ="top" align = "left">

<div id="progress">
<%= f.label :progress %><br>
<%= f.text_field :progress %></div>
</div>
<% end %>
</td>
</tr>
</table>
